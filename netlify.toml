[build.environment]
  # NEXT_PUBLIC_* values are embedded in client bundles by design — not secrets.
  SECRETS_SCAN_OMIT_KEYS = "NEXT_PUBLIC_CV_URL,NEXT_PUBLIC_SANITY_PROJECT_ID,NEXT_PUBLIC_SANITY_DATASET,NEXT_PUBLIC_SANITY_API_VERSION"
